# Producto pagado — NO publicar el PDF en el repo público
*.pdf

# Fuentes de trabajo / procesamiento de recursos
_src/
node_modules/
package.json
package-lock.json

# Video crudo (footage sin comprimir) — nunca al repo, muy pesado
*.mov
*.MOV

# Sistema
.DS_Store
Thumbs.db

.vercel
